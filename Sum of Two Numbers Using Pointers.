#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int sum=0;
    int *ptr1=(int*)malloc(sizeof(int));
    int *ptr2=(int*)malloc(sizeof(int));
    scanf("%d",ptr1);
    scanf("%d",ptr2);
    sum=*ptr1+*ptr2;
    printf("%d", sum);
    return 0;
}
