#include<stdio.h>
int isprime(int n){
    if(n ==1)
        return 0;
  for(int i=2;i<=n/2;i++){
      if(n%i==0)
          return 0;
  }
    return 1;
}
int main(){
    int x,y,flag=0;
    scanf("%d%d",&x,&y);
    for(int i=x;i<y;i++){
        if(isprime(i)){
            printf("%d ", i);
            flag=1;
        }
    }
    if(flag==0)
        printf("No prime numbers found");
    return 0;
}
