#include <stdio.h>
int main() {
    int i, n,num,max;
    scanf("%d",&n);
    scanf("%d",&max);
    for(i=1;i<n;i++){
        scanf("%d",&num);
        if(max<num){
            max=num;
        }
    }
    printf("%d",max);
    return 0;
}
