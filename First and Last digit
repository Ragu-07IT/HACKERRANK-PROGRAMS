#include <stdio.h>
int main() {
    int n,first,last;
    scanf("%d",&n);
    last=n%10;
    
    first=n;
    while(first>=10){
        first=first /10;
    }
    printf("%d %d",first,last);
          return 0;
}
